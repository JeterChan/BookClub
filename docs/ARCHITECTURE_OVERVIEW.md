# 系統架構概覽

## 📐 整體架構圖

```
┌─────────────────────────────────────────────────────────────┐
│                        使用者端                               │
│                    (Web Browser)                             │
└─────────────────┬───────────────────────────────────────────┘
                  │ HTTPS
                  │
┌─────────────────▼───────────────────────────────────────────┐
│                     前端應用                                  │
│              (Vite + React 19)                               │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Pages      │  │   Components │  │    Store     │     │
│  │              │  │              │  │   (Zustand)  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │          Services (API Client - Axios)                │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  部署: Vercel                                                │
└─────────────────┬───────────────────────────────────────────┘
                  │ REST API (JSON)
                  │ JWT Bearer Token
┌─────────────────▼───────────────────────────────────────────┐
│                     後端 API                                  │
│                (FastAPI + Python 3.11)                       │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              API Endpoints (路由層)                    │  │
│  │  - auth       - clubs      - events                   │  │
│  │  - users      - discussions - notifications           │  │
│  └──────────────────────────────────────────────────────┘  │
│                          ▼                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Services (業務邏輯層)                     │  │
│  └──────────────────────────────────────────────────────┘  │
│                          ▼                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           Models (SQLModel ORM)                       │  │
│  │  - User       - BookClub    - Event                   │  │
│  │  - Discussion - Notification - etc.                   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Security   │  │   Alembic    │  │  External    │     │
│  │  (JWT/bcrypt)│  │  (Migration) │  │   Services   │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                         - Cloudinary         │
│                                         - SendGrid           │
│  部署: Render                                                │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│                   資料庫層                                    │
│              PostgreSQL 15                                   │
│                                                              │
│  Tables:                                                     │
│  - user                - bookclub                            │
│  - interesttag         - discussion_topic                    │
│  - event               - notification                        │
│  - book_club_member    - event_participant                   │
│  - user_interest_tag   - book_club_tag_link                  │
│                                                              │
│  部署: Render PostgreSQL                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎨 前端架構

### 技術棧
- **框架**: React 19 + TypeScript
- **建置工具**: Vite 7
- **狀態管理**: Zustand 5.0
- **路由**: React Router DOM 7
- **樣式**: Tailwind CSS 4
- **HTTP**: Axios
- **表單**: React Hook Form + Zod
- **測試**: Vitest + React Testing Library

### 核心結構
```
frontend/src/
├── pages/           # 頁面元件
│   ├── HomePage.tsx
│   ├── LoginPage.tsx
│   ├── ClubPage.tsx
│   └── ...
├── components/      # 可重用元件
│   ├── ui/          # 基礎 UI
│   └── layout/      # 佈局
├── store/           # Zustand 狀態管理
│   ├── authStore.ts
│   ├── clubStore.ts
│   └── ...
├── services/        # API 服務層
│   ├── apiClient.ts
│   ├── authService.ts
│   └── ...
└── App.tsx          # 路由設定
```

### 資料流向
```
User Action → Component → Store (Zustand) → Service (Axios)
                ▲                                    │
                │                                    ▼
                └──────────── Response ──────── Backend API
```

---

## ⚙️ 後端架構

### 技術棧
- **框架**: FastAPI + Uvicorn
- **ORM**: SQLModel
- **資料庫**: PostgreSQL 15
- **遷移**: Alembic
- **認證**: JWT (python-jose) + bcrypt
- **測試**: pytest + httpx

### 核心結構
```
backend/app/
├── api/
│   └── endpoints/   # API 端點
│       ├── auth.py       # 認證
│       ├── users.py      # 使用者
│       ├── book_clubs.py # 讀書會
│       ├── events.py     # 活動
│       └── ...
├── models/          # SQLModel 模型
│   ├── user.py
│   ├── book_club.py
│   ├── event.py
│   └── ...
├── core/            # 核心功能
│   ├── config.py
│   └── security.py  # JWT, 密碼處理
├── db/
│   └── session.py   # 資料庫連線
└── main.py          # FastAPI 應用入口
```

### 請求處理流程
```
HTTP Request
    │
    ▼
FastAPI Router (api/endpoints)
    │
    ▼
Business Logic / Services
    │
    ▼
SQLModel ORM
    │
    ▼
PostgreSQL Database
    │
    ▼
Response (JSON)
```

---

## 🔐 認證流程

```
┌──────────┐                    ┌──────────┐                    ┌──────────┐
│  前端     │                    │  後端     │                    │  資料庫   │
└────┬─────┘                    └────┬─────┘                    └────┬─────┘
     │                                │                                │
     │ 1. POST /auth/login            │                                │
     │ (email, password)              │                                │
     ├───────────────────────────────>│                                │
     │                                │ 2. 驗證密碼                     │
     │                                ├───────────────────────────────>│
     │                                │<───────────────────────────────┤
     │                                │ 3. 產生 JWT token              │
     │ 4. 回傳 token                   │                                │
     │<───────────────────────────────┤                                │
     │ 5. 儲存 token (localStorage)   │                                │
     │                                │                                │
     │ 6. API Request                 │                                │
     │ (Authorization: Bearer token)  │                                │
     ├───────────────────────────────>│                                │
     │                                │ 7. 驗證 token                  │
     │                                │ 8. 取得資料                     │
     │                                ├───────────────────────────────>│
     │                                │<───────────────────────────────┤
     │ 9. 回傳資料                     │                                │
     │<───────────────────────────────┤                                │
```

---

## 📊 資料庫 ERD (簡化版)

```
┌─────────────┐         ┌─────────────────┐         ┌─────────────┐
│    User     │         │  BookClubMember │         │  BookClub   │
├─────────────┤         ├─────────────────┤         ├─────────────┤
│ id (PK)     │────────<│ user_id (FK)    │>────────│ id (PK)     │
│ email       │         │ club_id (FK)    │         │ name        │
│ password    │         │ role            │         │ owner_id    │
│ display_name│         │ joined_at       │         │ description │
└─────┬───────┘         └─────────────────┘         └─────┬───────┘
      │                                                     │
      │ owns                                                │
      └─────────────────────────────────────────────────────┘
      │
      │ participates_in
      │
      ▼
┌─────────────────┐         ┌─────────────┐
│ EventParticipant│         │    Event    │
├─────────────────┤         ├─────────────┤
│ user_id (FK)    │>────────│ id (PK)     │
│ event_id (FK)   │         │ club_id (FK)│
│ status          │         │ title       │
└─────────────────┘         │ event_datetime│
                            └─────────────┘
      │
      │ has
      ▼
┌─────────────────┐
│  Notification   │
├─────────────────┤
│ id (PK)         │
│ recipient_id(FK)│
│ type            │
│ content (JSON)  │
└─────────────────┘
```

---

## 🚀 部署架構

```
┌─────────────────────────────────────────────────────────────┐
│                        GitHub Repository                     │
└────────────┬────────────────────────────────────┬───────────┘
             │                                     │
             │ Auto Deploy                         │ Auto Deploy
             │                                     │
┌────────────▼──────────┐              ┌──────────▼────────────┐
│       Vercel          │              │       Render          │
│   (Frontend Host)     │              │   (Backend Host)      │
│                       │              │                       │
│  - 自動建置            │              │  - 自動建置            │
│  - 全球 CDN           │  REST API    │  - 執行 Alembic       │
│  - Preview Deploys    │<────────────>│  - Uvicorn Server     │
│                       │   HTTPS      │                       │
└───────────────────────┘              └──────────┬────────────┘
                                                  │
                                       ┌──────────▼────────────┐
                                       │  Render PostgreSQL    │
                                       │   (Managed Database)  │
                                       │                       │
                                       │  - 自動備份            │
                                       │  - 監控               │
                                       └───────────────────────┘

外部服務:
┌─────────────┐  ┌─────────────┐
│ Cloudinary  │  │  SendGrid   │
│ (圖片儲存)   │  │  (Email)    │
└─────────────┘  └─────────────┘
```

---

## 📋 API 端點概覽

### 認證 (Auth)
- `POST /api/v1/auth/register` - 註冊
- `POST /api/v1/auth/login` - 登入

### 使用者 (Users)
- `GET /api/v1/users/me` - 取得個人資料
- `PUT /api/v1/users/me` - 更新個人資料
- `PUT /api/v1/users/me/interest-tags` - 更新興趣標籤

### 讀書會 (Book Clubs)
- `GET /api/v1/clubs` - 探索讀書會
- `POST /api/v1/clubs` - 建立讀書會
- `GET /api/v1/clubs/{id}` - 讀書會詳情
- `POST /api/v1/clubs/{id}/join` - 申請加入

### 活動 (Events)
- `GET /api/v1/clubs/{id}/events` - 取得活動列表
- `POST /api/v1/clubs/{id}/events` - 建立活動
- `POST /api/v1/events/{id}/register` - 報名活動

### 討論 (Discussions)
- `GET /api/v1/clubs/{id}/discussions` - 討論列表
- `POST /api/v1/clubs/{id}/discussions` - 建立討論
- `POST /api/v1/clubs/{id}/discussions/{tid}/comments` - 回覆

### 通知 (Notifications)
- `GET /api/v1/notifications` - 通知列表
- `POST /api/v1/notifications/read-all` - 全部已讀

---

## 🔧 開發環境

### 本地開發啟動

**後端**:
```bash
cd backend
docker-compose up --build
# API: http://localhost:8000
# Docs: http://localhost:8000/docs
```

**前端**:
```bash
cd frontend
npm install
npm run dev
# App: http://localhost:5173
```

### 環境變數

**後端 (.env)**:
```bash
DATABASE_URL=postgresql://user:pass@db:5432/bookclub_dev
SECRET_KEY=your-secret-key
FRONTEND_URL=http://localhost:5173
```

**前端 (.env.local)**:
```bash
VITE_API_BASE_URL=http://localhost:8000
```

---

**文件版本**: v1.0  
**更新日期**: 2025-11-11  
**維護者**: Winston (Architect)
