# Story 1.0-Frontend: Header 與 Footer 共用佈局元件

**Status**: Approved

---

## Story

**As a** 前端開發者與用戶,
**I want** 在所有頁面中都有一致的 Header 與 Footer 元件,
**so that** 提供統一的導航體驗和應用佈局架構

---

## Acceptance Criteria

1. Header 元件顯示 Logo、主導航選單、用戶資訊（認證後）
2. Header 根據認證狀態顯示不同的導航選項（未登入 vs 已登入）
3. Header 在手機版會收合成 Hamburger 選單
4. Footer 元件顯示版權資訊、政策連結、社交媒體連結
5. 建立 Layout 元件整合 Header 和 Footer，供所有頁面使用
6. Header 的用戶下拉選單包含：個人檔案、設定、登出功能
7. 所有元件在手機、平板、桌面都完美顯示（RWD）

---

## Dev Notes

### Story Context

這是 Epic 1 前端開發的**第一個且最基礎的 Story**。所有後續的頁面（註冊、登入、儀表板、個人檔案）都將使用這個共用佈局。

**開發順序建議：**
1. Story 1.0-Frontend (本故事) - Header/Footer 共用佈局 ✋ **START HERE**
2. Story 1.1-Frontend - 註冊頁面
3. Story 1.2-Frontend - 登入頁面
4. Story 1.3-Frontend - 儀表板頁面
5. Story 1.4-Frontend - 個人檔案頁面

### Previous Story Insights

無前置故事（本故事為 Epic 1 Frontend 起點）

**重要技術決策：**
- 前端使用 **React 18 + TypeScript + Vite**
- 狀態管理使用 **Redux Toolkit**
- 樣式使用 **Tailwind CSS**
- 路由使用 **React Router DOM v6**
- [Source: docs/architecture/5-前端架構詳細設計-vite-react.md]

### Architecture Context: Frontend Project Structure

[Source: docs/architecture/5-前端架構詳細設計-vite-react.md]

```
/frontend/src/
├── components/
│   ├── common/          # 共用元件（Header, Footer, Layout）
│   ├── ui/              # 基礎 UI 元件（Button, Input 等）
│   └── ...
├── pages/               # 頁面元件
├── store/               # Redux Toolkit store
│   ├── slices/          # Redux slices（authSlice, userSlice）
│   └── store.ts
├── types/               # TypeScript 型別定義
├── utils/               # 工具函式
├── App.tsx              # 主應用元件（路由設定）
└── main.tsx             # 應用進入點
```

### Data Models

#### User State (Redux Store)
[Source: docs/architecture/5-前端架構詳細設計-vite-react.md, backend/app/models/user.py]

```typescript
// src/types/auth.ts
interface User {
  id: number;
  email: string;
  display_name: string;
  avatar_url?: string;
  bio?: string;
  is_active: boolean;
}

interface AuthState {
  isAuthenticated: boolean;
  user: User | null;
  accessToken: string | null;
  refreshToken: string | null;
  loading: boolean;
  error: string | null;
}
```

#### Navigation Items Structure

```typescript
// src/types/navigation.ts
interface NavItem {
  label: string;
  path: string;
  icon?: React.ComponentType;  // 可選的圖示元件
  requireAuth: boolean;         // 是否需要認證
  mobileOnly?: boolean;         // 僅在手機版顯示
}

interface UserMenuItem {
  label: string;
  path?: string;                // 內部連結
  onClick?: () => void;         // 或點擊事件（如登出）
  icon?: React.ComponentType;
  divider?: boolean;            // 是否為分隔線
}
```

### Component Specifications

#### Layout Component
[Source: docs/ui-specs/epic1-pages-summary.md, docs/prd/4-uiux-設計目標.md]

**檔案位置：** `frontend/src/components/common/Layout.tsx`

**Props:**
```typescript
interface LayoutProps {
  children: React.ReactNode;
  showHeader?: boolean;    // 預設 true
  showFooter?: boolean;    // 預設 true
  maxWidth?: 'sm' | 'md' | 'lg' | 'xl' | 'full';  // 預設 'xl'
  className?: string;
}
```

**職責：**
- 整合 Header 和 Footer 元件
- 提供統一的頁面容器和最大寬度控制
- 處理內容區域的 padding 和 spacing

#### Header Component
[Source: docs/prd/4-uiux-設計目標.md]

**檔案位置：** `frontend/src/components/common/Header.tsx`

**視覺風格：**
- 主色調：深藍色系 (#1E40AF)
- 高度：64px (桌面), 56px (手機)
- 固定在頂部（`sticky top-0`）
- 白色背景 + 底部陰影

**桌面版佈局 (≥1024px):**
```
┌─────────────────────────────────────────────────────────┐
│ [Logo]  [導航選單...]              [用戶選單 ▼] [通知 🔔] │
└─────────────────────────────────────────────────────────┘
```

**手機版佈局 (<1024px):**
```
┌─────────────────────────────────────────────┐
│ [☰]  [Logo]              [用戶頭像] [通知 🔔] │
└─────────────────────────────────────────────┘
```

**導航選單項目：**

**未認證狀態：**
- 首頁 (`/`)
- 關於我們 (`/about`)
- 登入 (`/login`)
- 註冊 (`/register`)

**已認證狀態：**
- 儀表板 (`/dashboard`)
- 讀書會 (`/clubs`)
- 探索 (`/search`)
- 通知 (`/notifications`) - 僅圖示按鈕

**用戶下拉選單（已認證）：**
- 個人檔案 (`/profile`)
- 設定 (`/settings`)
- ─────────── (分隔線)
- 登出 (觸發 logout action)

**手機版 Hamburger 選單：**
- 點擊 ☰ 圖示展開側邊欄（Drawer）
- 顯示完整導航選單
- 背景遮罩（Overlay）點擊關閉

#### Footer Component
[Source: docs/prd/4-uiux-設計目標.md]

**檔案位置：** `frontend/src/components/common/Footer.tsx`

**視覺風格：**
- 淺灰色背景 (#F9FAFB)
- 上方邊界線
- Padding: 40px (桌面), 24px (手機)

**桌面版佈局 (≥768px):**
```
┌──────────────────────────────────────────────────────────────┐
│  關於我們  |  服務條款  |  隱私政策  |  聯絡我們              │
│                                                                │
│  © 2025 讀書會平台. All rights reserved.   [FB] [IG] [Twitter] │
└──────────────────────────────────────────────────────────────┘
```

**手機版佈局 (<768px):**
```
┌────────────────────────┐
│  關於我們              │
│  服務條款              │
│  隱私政策              │
│  聯絡我們              │
│                        │
│  [FB] [IG] [Twitter]   │
│                        │
│  © 2025 讀書會平台.    │
│  All rights reserved.  │
└────────────────────────┘
```

**Footer 連結項目：**
- 關於我們 (`/about`)
- 服務條款 (`/terms`)
- 隱私政策 (`/privacy`)
- 聯絡我們 (`/contact`)
- 社交媒體：Facebook, Instagram, Twitter（外部連結）

### File Locations and Project Structure

[Source: docs/architecture/5-前端架構詳細設計-vite-react.md]

**需要創建的檔案：**

1. **共用佈局元件：**
   - `frontend/src/components/common/Layout.tsx` - 主佈局元件
   - `frontend/src/components/common/Header.tsx` - Header 元件
   - `frontend/src/components/common/Footer.tsx` - Footer 元件
   - `frontend/src/components/common/MobileMenu.tsx` - 手機版側邊選單

2. **型別定義：**
   - `frontend/src/types/navigation.ts` - 導航相關型別

3. **Redux Store（如尚未建立）：**
   - `frontend/src/store/slices/authSlice.ts` - 認證狀態管理
   - `frontend/src/store/store.ts` - Redux store 設定

4. **工具函式：**
   - `frontend/src/utils/cn.ts` - Tailwind class 合併工具（使用 clsx + tailwind-merge）

**需要修改的檔案：**

5. **App.tsx：**
   - 整合 Layout 元件到路由結構
   - 確保所有頁面都使用統一佈局

### Technical Constraints and Implementation Notes

#### Responsive Design Breakpoints
[Source: docs/prd/4-uiux-設計目標.md]

- **手機版 (sm):** 320-767px
- **平板版 (md):** 768-1023px
- **桌面版 (lg/xl):** 1024px+

**Tailwind 斷點使用：**
```tsx
// 手機版預設，桌面版覆蓋
<div className="flex flex-col md:flex-row lg:gap-6">
```

#### State Management Strategy
[Source: docs/architecture/5-前端架構詳細設計-vite-react.md]

**Redux Toolkit - authSlice:**

需要實作的 selectors:
```typescript
// src/store/slices/authSlice.ts
export const selectIsAuthenticated = (state: RootState) => state.auth.isAuthenticated;
export const selectCurrentUser = (state: RootState) => state.auth.user;
export const selectAuthLoading = (state: RootState) => state.auth.loading;
```

需要實作的 actions:
```typescript
// Async thunk for logout
export const logout = createAsyncThunk(
  'auth/logout',
  async (_, { rejectWithValue }) => {
    try {
      // 清除 localStorage tokens
      localStorage.removeItem('access_token');
      localStorage.removeItem('refresh_token');
      // 可選：呼叫 backend logout API
      return;
    } catch (error) {
      return rejectWithValue('Logout failed');
    }
  }
);
```

**Header 元件使用範例：**
```tsx
import { useSelector, useDispatch } from 'react-redux';
import { selectIsAuthenticated, selectCurrentUser, logout } from '@/store/slices/authSlice';

function Header() {
  const isAuthenticated = useSelector(selectIsAuthenticated);
  const user = useSelector(selectCurrentUser);
  const dispatch = useDispatch();

  const handleLogout = () => {
    dispatch(logout());
  };

  // ...render logic
}
```

#### Navigation with React Router
[Source: docs/architecture/5-前端架構詳細設計-vite-react.md]

**使用 `<Link>` 取代 `<a>`：**
```tsx
import { Link } from 'react-router-dom';

<Link to="/dashboard" className="nav-link">
  儀表板
</Link>
```

**程式化導航（登出後）：**
```tsx
import { useNavigate } from 'react-router-dom';

const navigate = useNavigate();

const handleLogout = async () => {
  await dispatch(logout());
  navigate('/login');
};
```

#### Styling with Tailwind CSS
[Source: docs/architecture/5-前端架構詳細設計-vite-react.md]

**工具函式：`cn()` (Class Names)**

安裝依賴（若尚未安裝）：
```bash
npm install clsx tailwind-merge
```

建立工具函式：
```typescript
// src/utils/cn.ts
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**使用範例：**
```tsx
import { cn } from '@/utils/cn';

<div className={cn(
  "base-class",
  isActive && "active-class",
  className  // 從 props 傳入
)}>
```

#### Accessibility Considerations

**鍵盤導航：**
- 所有互動元素可用 Tab 鍵導航
- 下拉選單支援 Escape 鍵關閉
- 手機選單支援 Escape 鍵關閉

**ARIA 屬性：**
```tsx
// Hamburger 按鈕
<button 
  aria-label="開啟選單"
  aria-expanded={isMobileMenuOpen}
>

// 下拉選單
<div role="menu" aria-labelledby="user-menu-button">
```

**Focus 管理：**
- 開啟下拉選單時，focus 移至第一個項目
- 關閉選單時，focus 回到觸發按鈕

### Security Considerations

1. **XSS 防護：**
   - 不直接渲染 `user.display_name` HTML（React 預設已轉義）
   - 用戶頭像 URL 驗證（僅允許 https:// 或內部路徑）

2. **CSRF 保護：**
   - 登出請求應包含 CSRF token（若後端要求）

3. **Token 安全：**
   - 不在 URL 中傳遞 access token
   - 使用 `localStorage` 或 `sessionStorage` 儲存 token

### Testing Requirements

[Source: docs/architecture/2-後端架構詳細設計-sqlmodel.md#測試策略]

⚠️ **本故事必須包含測試**

#### 單元測試 (必須)

**測試檔案位置：**
- `frontend/src/components/common/__tests__/Layout.test.tsx`
- `frontend/src/components/common/__tests__/Header.test.tsx`
- `frontend/src/components/common/__tests__/Footer.test.tsx`
- `frontend/src/components/common/__tests__/MobileMenu.test.tsx`

**測試框架：**
- **Vitest** (Vite 原生支援)
- **React Testing Library** (@testing-library/react)

**測試重點：**

1. **Layout 元件測試：**
   - ✓ 正確渲染 children
   - ✓ 根據 props 顯示/隱藏 Header 和 Footer
   - ✓ maxWidth props 正確套用 class

2. **Header 元件測試：**
   - ✓ 未認證狀態顯示「登入」「註冊」連結
   - ✓ 已認證狀態顯示用戶名稱和頭像
   - ✓ 點擊用戶選單展開下拉選單
   - ✓ 點擊登出按鈕觸發 logout action
   - ✓ 手機版點擊 Hamburger 開啟側邊選單

3. **Footer 元件測試：**
   - ✓ 顯示所有導航連結
   - ✓ 顯示版權資訊
   - ✓ 社交媒體連結正確

4. **MobileMenu 元件測試：**
   - ✓ 開啟/關閉動畫正確
   - ✓ 點擊遮罩關閉選單
   - ✓ 按 Escape 鍵關閉選單

#### 整合測試（可選，留待後續 Story）

- 與實際頁面整合測試（在 Story 1.1-1.4 中進行）

#### 測試範例

```tsx
// frontend/src/components/common/__tests__/Header.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { Provider } from 'react-redux';
import { BrowserRouter } from 'react-router-dom';
import { configureStore } from '@reduxjs/toolkit';
import Header from '../Header';
import authReducer from '@/store/slices/authSlice';

const mockStore = (isAuthenticated = false, user = null) => {
  return configureStore({
    reducer: { auth: authReducer },
    preloadedState: {
      auth: {
        isAuthenticated,
        user,
        accessToken: null,
        refreshToken: null,
        loading: false,
        error: null
      }
    }
  });
};

describe('Header Component', () => {
  it('renders login and register links when not authenticated', () => {
    const store = mockStore(false);
    render(
      <Provider store={store}>
        <BrowserRouter>
          <Header />
        </BrowserRouter>
      </Provider>
    );

    expect(screen.getByText('登入')).toBeInTheDocument();
    expect(screen.getByText('註冊')).toBeInTheDocument();
  });

  it('renders user menu when authenticated', () => {
    const store = mockStore(true, { 
      id: 1, 
      email: 'test@example.com', 
      display_name: 'Test User' 
    });
    
    render(
      <Provider store={store}>
        <BrowserRouter>
          <Header />
        </BrowserRouter>
      </Provider>
    );

    expect(screen.getByText('Test User')).toBeInTheDocument();
  });

  it('opens user dropdown on click', () => {
    const store = mockStore(true, { 
      id: 1, 
      email: 'test@example.com', 
      display_name: 'Test User' 
    });
    
    render(
      <Provider store={store}>
        <BrowserRouter>
          <Header />
        </BrowserRouter>
      </Provider>
    );

    const userMenuButton = screen.getByRole('button', { name: /Test User/i });
    fireEvent.click(userMenuButton);

    expect(screen.getByText('個人檔案')).toBeInTheDocument();
    expect(screen.getByText('登出')).toBeInTheDocument();
  });
});
```

**執行測試：**
```bash
npm run test
# 或
npm run test:watch  # 監聽模式
```

---

## Tasks / Subtasks

### Task 1: 專案設定與依賴安裝 (AC: All)
- [ ] 1.1 確認 React Router DOM 已安裝
  - 執行：`npm install react-router-dom`
- [ ] 1.2 確認 Redux Toolkit 已安裝
  - 執行：`npm install @reduxjs/toolkit react-redux`
- [ ] 1.3 安裝 Tailwind class 合併工具
  - 執行：`npm install clsx tailwind-merge`
- [ ] 1.4 安裝測試依賴（如尚未安裝）
  - 執行：`npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom`
- [ ] 1.5 設定 Vitest 配置（修改 `vite.config.ts`）
  - 加入 test environment: jsdom

### Task 2: 工具函式與型別定義 (AC: All)
- [ ] 2.1 創建 `frontend/src/utils/cn.ts`
  - 實作 `cn()` 函式（clsx + tailwind-merge）
- [ ] 2.2 創建 `frontend/src/types/navigation.ts`
  - 定義 `NavItem` interface
  - 定義 `UserMenuItem` interface
- [ ] 2.3 創建 `frontend/src/types/auth.ts`（如尚未存在）
  - 定義 `User` interface
  - 定義 `AuthState` interface

### Task 3: Redux Store - Auth Slice (AC: 2, 6)
- [ ] 3.1 創建 `frontend/src/store/slices/authSlice.ts`
  - 定義 initial state（isAuthenticated, user, tokens）
  - 實作 `logout` async thunk
  - 實作 reducers（login success, logout success）
  - 匯出 selectors（selectIsAuthenticated, selectCurrentUser）
- [ ] 3.2 修改 `frontend/src/store/store.ts`（如尚未存在則創建）
  - 設定 configureStore 並註冊 authSlice
  - 匯出 RootState 和 AppDispatch 型別
- [ ] 3.3 修改 `frontend/src/main.tsx`
  - 使用 `<Provider>` 包裝 `<App />`

### Task 4: Footer 元件實作 (AC: 4, 7)
- [ ] 4.1 創建 `frontend/src/components/common/Footer.tsx`
  - 實作 Footer 元件骨架
- [ ] 4.2 實作桌面版佈局（grid 3 欄位）
  - 左側：導航連結（關於我們、服務條款等）
  - 中間：版權資訊
  - 右側：社交媒體圖示
- [ ] 4.3 實作手機版佈局（flex-col 垂直排列）
  - 使用 `md:` 斷點切換佈局
- [ ] 4.4 新增 Tailwind 樣式
  - 背景色：`bg-gray-50`
  - 上方邊界：`border-t border-gray-200`
  - Padding：`py-8 px-4 md:px-6`

### Task 5: Header 元件實作 - 桌面版 (AC: 1, 2, 6)
- [ ] 5.1 創建 `frontend/src/components/common/Header.tsx`
  - 實作 Header 元件骨架
  - 使用 `useSelector` 取得認證狀態和用戶資訊
- [ ] 5.2 實作 Logo 區域
  - 使用 `<Link to="/">` 連結到首頁
  - Logo 圖片或文字顯示
- [ ] 5.3 實作導航選單（桌面版）
  - 根據 `isAuthenticated` 顯示不同選單項目
  - 使用 `<Link>` 元件
  - 樣式：水平排列，hover 效果
- [ ] 5.4 實作右側區域（桌面版）
  - 未認證：登入、註冊按鈕
  - 已認證：通知圖示 + 用戶選單按鈕
- [ ] 5.5 實作用戶下拉選單
  - 使用 state 控制開啟/關閉
  - 點擊外部關閉（使用 useEffect + document.addEventListener）
  - 選單項目：個人檔案、設定、分隔線、登出
  - 登出按鈕觸發 `dispatch(logout())`

### Task 6: Header 元件實作 - 手機版 (AC: 3, 7)
- [ ] 6.1 新增 Hamburger 圖示按鈕（手機版顯示）
  - 使用 `lg:hidden` class（桌面版隱藏）
  - 點擊觸發 `setIsMobileMenuOpen(true)`
- [ ] 6.2 實作 MobileMenu 元件（側邊 Drawer）
  - 創建 `frontend/src/components/common/MobileMenu.tsx`
  - 使用 state 控制開啟/關閉
  - 背景遮罩（Overlay）點擊關閉
- [ ] 6.3 實作 MobileMenu 內容
  - 顯示完整導航選單（垂直排列）
  - 根據認證狀態顯示不同項目
  - 用戶資訊區域（已認證時）
- [ ] 6.4 新增動畫效果
  - 側邊欄滑入/滑出動畫（Tailwind `transition-transform`）
  - 遮罩淡入/淡出動畫

### Task 7: Layout 元件實作 (AC: 5, 7)
- [ ] 7.1 創建 `frontend/src/components/common/Layout.tsx`
  - 定義 LayoutProps interface
  - 實作 Layout 元件骨架
- [ ] 7.2 整合 Header 和 Footer
  - 根據 `showHeader` 和 `showFooter` props 條件渲染
- [ ] 7.3 實作內容容器
  - 使用 `<main>` 標籤包裝 children
  - 根據 `maxWidth` props 設定最大寬度
  - 加入適當 padding 和 min-height
- [ ] 7.4 實作 RWD 樣式
  - 手機版：全寬，較少 padding
  - 平板/桌面：置中，較多 padding

### Task 8: App.tsx 整合 (AC: 5)
- [ ] 8.1 修改 `frontend/src/App.tsx`
  - 匯入 Layout 元件
  - 將現有路由用 Layout 包裝
- [ ] 8.2 設定不需要 Layout 的路由（可選）
  - 登入頁面和註冊頁面可使用簡化佈局
  - 使用不同的 Layout variant 或條件渲染

### Task 9: 樣式優化與 RWD 調整 (AC: 7)
- [ ] 9.1 測試所有斷點的顯示效果
  - 手機版 (375px, 390px, 430px)
  - 平板版 (768px, 820px)
  - 桌面版 (1024px, 1280px, 1920px)
- [ ] 9.2 調整 Header 高度
  - 桌面：64px
  - 手機：56px
- [ ] 9.3 確保文字大小在所有裝置都易讀
  - 最小字體：14px (0.875rem)
- [ ] 9.4 測試 touch 目標大小（手機版）
  - 所有可點擊元素至少 44x44px

### Task 10: 單元測試 - Layout (AC: 5)
- [ ] 10.1 創建 `frontend/src/components/common/__tests__/Layout.test.tsx`
  - 測試 children 正確渲染
  - 測試 showHeader=false 時不顯示 Header
  - 測試 showFooter=false 時不顯示 Footer
  - 測試 maxWidth props 正確套用 class

### Task 11: 單元測試 - Header (AC: 1, 2, 3, 6)
- [ ] 11.1 創建 `frontend/src/components/common/__tests__/Header.test.tsx`
  - 設定 mock Redux store
  - 設定 BrowserRouter wrapper
- [ ] 11.2 測試未認證狀態
  - 顯示「登入」「註冊」連結
  - 不顯示用戶選單
- [ ] 11.3 測試已認證狀態
  - 顯示用戶名稱和頭像
  - 顯示通知圖示
  - 不顯示「登入」「註冊」連結
- [ ] 11.4 測試用戶下拉選單
  - 點擊用戶選單按鈕展開下拉選單
  - 下拉選單包含「個人檔案」「設定」「登出」
  - 點擊登出觸發 logout action
- [ ] 11.5 測試手機版
  - Hamburger 按鈕在手機版顯示
  - 點擊 Hamburger 開啟 MobileMenu

### Task 12: 單元測試 - Footer (AC: 4)
- [ ] 12.1 創建 `frontend/src/components/common/__tests__/Footer.test.tsx`
  - 測試所有導航連結存在
  - 測試版權資訊顯示
  - 測試社交媒體連結正確

### Task 13: 單元測試 - MobileMenu (AC: 3)
- [ ] 13.1 創建 `frontend/src/components/common/__tests__/MobileMenu.test.tsx`
  - 測試開啟/關閉狀態
  - 測試點擊遮罩關閉選單
  - 測試按 Escape 鍵關閉選單
  - 測試導航項目正確顯示

### Task 14: 執行測試與覆蓋率檢查 (AC: All)
- [ ] 14.1 執行所有單元測試
  - 指令：`npm run test`
  - 確保所有測試通過
- [ ] 14.2 檢查測試覆蓋率
  - 指令：`npm run test:coverage`
  - 目標：元件覆蓋率 > 80%
- [ ] 14.3 修復失敗的測試
  - 分析失敗原因
  - 修正程式碼或測試

### Task 15: 瀏覽器手動測試 (AC: All)
- [ ] 15.1 測試所有認證狀態切換
  - 未登入 → 登入 → 登出
  - 檢查 Header 導航項目正確更新
- [ ] 15.2 測試所有連結
  - 點擊所有 Header 和 Footer 連結
  - 確認路由正確跳轉
- [ ] 15.3 測試手機版
  - 使用 Chrome DevTools 切換裝置
  - 測試 Hamburger 選單開啟/關閉
  - 測試用戶下拉選單
- [ ] 15.4 測試 RWD
  - 調整瀏覽器寬度從 320px 到 1920px
  - 確認所有斷點的佈局正確

### Task 16: Accessibility 檢查 (AC: All)
- [ ] 16.1 使用鍵盤導航測試
  - Tab 鍵可導航所有互動元素
  - Enter 鍵可觸發按鈕和連結
  - Escape 鍵可關閉選單
- [ ] 16.2 檢查 ARIA 屬性
  - 使用 axe DevTools 或 Lighthouse 檢查
  - 修正任何 Accessibility 問題
- [ ] 16.3 測試螢幕閱讀器（可選）
  - 使用 macOS VoiceOver 或 NVDA

### Task 17: 程式碼審查與優化 (AC: All)
- [ ] 17.1 審查所有程式碼的命名和結構
  - 確保元件命名清晰
  - 確保檔案位置符合專案結構
- [ ] 17.2 優化效能
  - 使用 React.memo 避免不必要的重新渲染（如適用）
  - 檢查是否有 console.log 或 debugger
- [ ] 17.3 確認 TypeScript 型別完整
  - 所有 props 都有型別定義
  - 無 `any` 型別（除非必要）
- [ ] 17.4 整理 import 順序
  - React imports 在前
  - 第三方套件
  - 專案內部 imports
- [ ] 17.5 新增註解（如需要）
  - 複雜邏輯加上說明註解
  - 元件功能 JSDoc 註解

### Task 18: 文件更新 (AC: All)
- [ ] 18.1 更新 `README.md`（如需要）
  - 記錄新增的元件
  - 更新開發指令
- [ ] 18.2 建立元件使用範例（可選）
  - 在 `docs/` 新增元件使用說明

---

## Testing

### Test File Locations

**單元測試：**
- `frontend/src/components/common/__tests__/Layout.test.tsx` - Layout 元件測試
- `frontend/src/components/common/__tests__/Header.test.tsx` - Header 元件測試
- `frontend/src/components/common/__tests__/Footer.test.tsx` - Footer 元件測試
- `frontend/src/components/common/__tests__/MobileMenu.test.tsx` - MobileMenu 元件測試

### Testing Standards

- 使用 **Vitest** 作為測試框架
- 使用 **React Testing Library** 進行元件測試
- 目標覆蓋率：80% 以上

### Testing Frameworks and Patterns

**測試環境設定：**
```typescript
// vite.config.ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: './src/test/setup.ts'
  }
});
```

**Setup 檔案：**
```typescript
// src/test/setup.ts
import '@testing-library/jest-dom';
```

**測試工具函式：**
```typescript
// src/test/test-utils.tsx
import { render } from '@testing-library/react';
import { Provider } from 'react-redux';
import { BrowserRouter } from 'react-router-dom';
import { configureStore } from '@reduxjs/toolkit';
import authReducer from '@/store/slices/authSlice';

export const renderWithProviders = (
  ui: React.ReactElement,
  preloadedState = {}
) => {
  const store = configureStore({
    reducer: { auth: authReducer },
    preloadedState
  });

  return render(
    <Provider store={store}>
      <BrowserRouter>
        {ui}
      </BrowserRouter>
    </Provider>
  );
};
```

### Specific Testing Requirements for This Story

1. **Layout 測試：**
   - 驗證 children 渲染
   - 驗證 Header/Footer 條件渲染
   - 驗證 maxWidth class 套用

2. **Header 測試：**
   - 認證狀態切換的 UI 變化
   - 用戶選單互動
   - 登出功能觸發
   - 手機版選單開啟

3. **Footer 測試：**
   - 所有連結存在
   - 版權資訊正確

4. **MobileMenu 測試：**
   - 開啟/關閉邏輯
   - Escape 鍵處理
   - 遮罩點擊處理

**執行測試指令：**
```bash
# 執行所有測試
npm run test

# 監聽模式
npm run test:watch

# 覆蓋率報告
npm run test:coverage
```

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-22 | 1.0 | Initial story creation for Header and Footer layout components | Scrum Master (Bob) |

---

## Dev Agent Record

### Agent Model Used
(To be filled by Dev Agent)

### Debug Log References
(To be filled by Dev Agent)

### Completion Notes
(To be filled by Dev Agent)

### File List
(To be filled by Dev Agent)

---

## QA Results
(To be filled by QA Agent)
